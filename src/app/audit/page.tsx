import { AuditPage } from "@/features/audit/audit-page";

export const metadata = {
  title: "Audit | AI Spend Audit",
  description: "Capture every AI tool in one pass.",
};

export default function AuditRoutePage() {
  return <AuditPage />;
}